name: Release CLI

on:
  push:
    tags:
      - "[0-9]*.[0-9]*.[0-9]*"

permissions:
  contents: write

jobs:
  build-macos:
    strategy:
      matrix:
        include:
          - runner: macos-latest
            asset: bip39kit-macos-arm64.tar.gz
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - run: swift build -c release
      - run: |
          mkdir -p dist
          cp .build/release/bip39kit dist/bip39kit
          cp -R .build/release/BIP39MnemonicKit_BIP39MnemonicKit.bundle dist/BIP39MnemonicKit_BIP39MnemonicKit.bundle
          tar -C dist -czf "${{ matrix.asset }}" bip39kit BIP39MnemonicKit_BIP39MnemonicKit.bundle
          shasum -a 256 "${{ matrix.asset }}" > "${{ matrix.asset }}.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: |
            ${{ matrix.asset }}
            ${{ matrix.asset }}.sha256

  publish:
    needs: build-macos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - run: |
          find artifacts -name "*.tar.gz" -exec cp {} . \;
          find artifacts -name "*.sha256" -exec cat {} \; > checksums.txt
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            bip39kit-*.tar.gz
            checksums.txt
